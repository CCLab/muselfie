<Page class="page" navigatingTo="onNavigatingTo" xmlns="http://schemas.nativescript.org/tns.xsd"
      xmlns:fresco="nativescript-fresco">
     <Page.actionBar >
        <ActionBar class="action-bar">
            <StackLayout class="steps" orientation="horizontal" horizontalAlignment="left">
                <Label text="start" visibility="{{ !chosenBackgroundPath ? 'visible' : 'collapse' }}"/>
                <Label class="action-bar-step mdi info" text="{{ fonticon('mdi-info') }}" visibility="{{ chosenBackgroundPath ? 'visible' : 'collapse' }}" />
                <Label class="action-bar-title" text="autor tytuł" visibility="{{ chosenBackgroundPath ? 'visible' : 'collapse' }}"/>
            </StackLayout>
        </ActionBar>
    </Page.actionBar>
   <GridLayout>
        <fresco:FrescoDrawee id="image-background" 
                                height="100%"
                                aspectRatio="1"
                                verticalAlignment="top"
                                imageUri="{{ chosenBackgroundPath }}"
                                placeholderImageUri="res://ic_photo_black_48dp"/>
        <StackLayout verticalAlignment="bottom">
                <FlexboxLayout class="navigation-buttons-container">
                    <Button class="btn-back mdi" text="{{ 'wybierz tło' + fonticon('mdi-arrow-forward') }}" tap="chooseTap" visibility="{{ chosenBackgroundPath  ? 'collapse' : 'visible' }}"/>
                    <Button class="btn-back only_icon mdi" text="{{ fonticon('mdi-arrow-back') }}" tap="chooseTap" visibility="{{ !chosenBackgroundPath  ? 'collapse' : 'visible' }}"/>
                    <Button class="btn-next mdi" text="{{ 'dalej' + fonticon('mdi-arrow-forward') }}" tap="nextTap" visibility="{{ !chosenBackgroundPath   ? 'collapse' : 'visible' }}"/>
                </FlexboxLayout>
        </StackLayout>
    </GridLayout>
</Page>
